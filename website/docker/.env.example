# Compose interpolation values. Copy this file beside the Compose file as `.env`
# or export these values in the shell before running Docker Compose.
DB_NAME=elanora
DB_USER=elanora
DB_PASSWORD=replace-with-a-long-random-password
